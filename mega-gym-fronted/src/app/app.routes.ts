import { Routes } from '@angular/router';
import { ClientesListaComponent } from './components/clientes-lista.component';

export const routes: Routes = [
    {
        path: 'clientes',
        component: ClientesListaComponent
    }
];
